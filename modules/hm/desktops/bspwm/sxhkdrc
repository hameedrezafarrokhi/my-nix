# Launch Rofi Application Menu
super + space
   	rofi -show drun -modi drun -line-padding 4 -hide-scrollbar -show-icons -theme /home/hrf/nixos/modules/hm/desktops/awesome/awesome/rofi/config.rasi

# Set Window State: Floating Mode
super + f
	bspc node -t ~floating

# Set Window State: Fullscreen Mode
super + Return
	bspc node -t fullscreen

# Set Window State: Fullscreen Mode
super + shift + Return
	bspc node -t ~tiled

# Cycle to next window (like next tab)
alt + Tab
      bspc node -f next.local.window

# Cycle to previous window (like previous tab)
alt + shift + Tab
      bspc node -f prev.local.window

# Shift to next window
super + Tab
      bspc node -s next.local.window

# Shift to previous window
super + shift + Tab
      bspc node -s prev.local.window

# Skippy-xd Expo
super + ctrl + Tab
      skippy-xd --expose

# Skippy-xd Pager
super + ctrl + shift + Tab
      skippy-xd --paging

# Reload sxhkd (Hotkey Daemon)
super + Escape
	pkill -USR1 -x sxhkd; notify-send 'sxhkd' 'Reloaded config'

# Reload bspwm (Window Manager)
alt + shift + r
	bspc wm -r; notify-send 'bspwm' 'Restarted'

# Quit bspwm (Exit Window Manager)
alt + shift + e
	bspc quit

# Close Focused Window
super + c
	bspc node -c

# Change Focus to Left Window
super + Left
	bspc node -f west

# Change Focus to Window Below
super + Down
	bspc node -f south

# Change Focus to Window Above
super + Up
	bspc node -f north

# Change Focus to Right Window
super + Right
	bspc node -f east

# Swap Focused Window with Left Window
super + shift + Left
	bspc node -s west

# Swap Focused Window with Window Below
super + shift + Down
	bspc node -s south

# Swap Focused Window with Window Above
super + shift + Up
	bspc node -s north

# Swap Focused Window with Right Window
super + shift + Right
	bspc node -s east

# Switch to Workspace 1-12
super + {1-9,0}
	bspc desktop --focus ^{1-9,10}

# Move Focused Window to Workspace 1-10
super + shift + {1-9,0}
	bspc node --to-desktop ^{1-9,10}

# Move Focused Window to Workspace 1-10 AND Follow
super + shift + ctrl + {1-9,0}
      bsp-send-follow {1-9,10}

# Expand window width right
super + ctrl + Right
	bspc node -z right 40 0

# Expand window width left
super + ctrl + Left
	bspc node -z left -40 0

# Shrink window width right
super + ctrl + alt + Right
	bspc node -z left 40 0

# Shrink window width left
super + ctrl + alt + Left
	bspc node -z right -40 0

# Expand window height up
super + ctrl + Up
	bspc node -z top 0 -40

# Expand window height down
super + ctrl + Down
	bspc node -z bottom 0 40

# Shrink window height up
super + ctrl + alt + Up
	bspc node -z bottom 0 -40

# Shrink window height down
super + ctrl + alt + Down
	bspc node -z top 0 40

# Expand window width both sides
super + ctrl + shift + Right
	bspc node -z right 40 0; bspc node -z left 40 0

# Shrink window width both sides
super + ctrl + shift + Left
	bspc node -z right -40 0; bspc node -z left -40 0

# Resize Window: Shrink Down
super + ctrl + shift + Down
	bspc node -z top 0 40; bspc node -z bottom 0 40

# Resize Window: Shrink Up
super + ctrl + shift + Up
	bspc node -z top 0 -40; bspc node -z bottom 0 -40

# Increase Volume
XF86AudioRaiseVolume
      wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+

# Decrease Volume
XF86AudioLowerVolume
      wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-

# Increase Volume (notify)
super + Insert
      bsp-volume up

# Decrease Volume (notify)
super + Delete
	bsp-volume down

# Mute Volume (notify)
super + Home
	bsp-volume mute

# Increase Screen Brightness
XF86MonBrightnessUp
	xbacklight +10

# Decrease Screen Brightness
XF86MonBrightnessDown
	xbacklight -10

# Take a Screenshot (Region Select)
super + Print
	flameshot gui --path ~/Pictures/Screenshots/

# Take a Fullscreen Screenshot
Print
	flameshot full --path ~/Pictures/Screenshots/

# Master Stack Layout
super + ctrl + 1
      bsp-cache-layout; bsp-layout next --layouts tall,rtall; notify-send "Master-Stack Layout"

# Wide Layout
super + ctrl + 2
      bsp-cache-layout; bsp-layout next --layouts wide,rwide; notify-send "Master-Stack Layout (Horizontal)"

# Monocle Layout
super + ctrl + 3
      bsp-cache-layout; bsp-layout set monocle; notify-send "Monocle Layout"

# Tiled Layout
super + ctrl + 4
      bsp-cache-layout; bsp-layout set tiled; notify-send "Tiled Layout"

# Grid Layout
super + ctrl + 5
      bsp-cache-layout; bsp-layout next --layouts grid,rgrid; notify-send "Grid Layout"

# Even Layout
super + ctrl + 6
      bsp-cache-layout; bsp-layout set even; notify-send "Even Layout"

# Floating Layout
super + ctrl + 7
	bsp-cache-layout; bsp-float; notify-send "Floating Layout"

# Resize All Windows Equally
super + ctrl + 8
	bsp-cache-layout; bspc query -N -d | xargs -I % bspc node % -B; notify-send "Equal Size Windows"

# Center Master Layout (Janky)
super + ctrl + 9
	bsp-cache-layout; bsp-cmaster-layout

# Master Stack Layout (OneShot)
super + alt + 1
      bsp-layout once tall; notify-send "(one-shot) Master-Stack Layout"

# Master Stack Layout (OneShot)
super + ctrl + alt + 1
      bsp-layout once rtall; notify-send "(one-shot) Master-Stack Layout"

# Master Stack Layout (Horizontal) (OneShot)
super + alt + 2
      bsp-layout once wide; notify-send "(one-shot) Master-Stack Layout (Horizontal)"

# Master Stack Layout (Horizontal) (OneShot)
super + ctrl + alt + 2
      bsp-layout once rwide; notify-send "(one-shot) Master-Stack Layout (Horizontal)"

# Master Stack Layout (OneShot)
super + alt + 5
      bsp-layout once grid; notify-send "(one-shot) Grid Layout"

# Master Stack Layout (OneShot)
super + ctrl + alt + 5
      bsp-layout once rgrid; notify-send "(one-shot) Grid Layout (Reverse)"

# Master Stack Layout (OneShot)
super + alt + 6
      bsp-layout once even; notify-send "(one-shot) Even Layout"

# Remove Current Layout and Restore Original Layout
super + ctrl + shift + BackSpace
	bsp-layout remove; bsp-restore-cached-layout; notify-send "Layout Restored"

# Remove Current Layout
super + ctrl + BackSpace
	bsp-layout remove; notify-send "Layout Removed"

# Remove Current Layout
super + ctrl + grave
	bsp-layout reload; notify-send "Reloaded Layouts"

# Floating Layout
super + shift + f
	bsp-float

# Keyboard layout change
alt + Caps_Lock
      setxkbmap -layout us,ir -option "grp:alt_caps_toggle"; notify-send "$(bsp-xkb-layout)"

# Reset Plank
alt + shift + p
      pkill plank && plank; notify-send 'Plank Reloaded'

# Keybind Help
super + h
      notify-send "I❤️BSPWM"; bsp-help

# Power Menu
ctrl + alt + Delete
      bsp-power

# Hide Bars
super + g
      bsp-bar-hide

# Hide Tint2
super + alt + g
      bsp-tint2-hide

# Hide Polybar
super + shift + g
      bsp-poly-hide

# Scratchpad Terminal
super + ctrl + Return
      {bspc rule -a \* -o state=floating sticky=on layer=above rectangle=720x360+320+60 && kitty}

# Scratchpad
super + ctrl + space
      scratchpad

# Gaps Increase
super + ctrl + equal
    bsp-gaps +

# Gaps Decrease
super + ctrl + minus
    bsp-gaps -

# Multiple Workspaces View
super + shift + grave
    rofi -dmenu -p "Desktops to toggle:" -theme ~/nixos/modules/hm/desktops/awesome/awesome/rofi/config.rasi | xargs bsp-tag-view

# Multiple Workspaces View Revert
super + shift + Escape
    bsp-tag-view-revert; notify-send "Sticky Tag View Reverted"

# Spawn Preselection Point Up
super + alt + Up
    bspc node --presel-dir \~north

# Spawn Preselection Point Down
super + alt + Down
    bspc node --presel-dir \~south

# Spawn Preselection Point Right
super + alt + Right
    bspc node --presel-dir \~east

# Spawn Preselection Point Left
super + alt + Left
    bspc node --presel-dir \~west

# Remove All Preselections
super + alt + Escape
    bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

# Spawn Empty Point Up
super + alt + shift + Up
    bspc node --presel-dir \~north -i

# Spawn Empty Point Down
super + alt + shift + Down
    bspc node --presel-dir \~south -i

# Spawn Empty Point Right
super + alt + shift + Right
    bspc node --presel-dir \~east -i

# Spawn Empty Point Left
super + alt + shift + Left
    bspc node --presel-dir \~west -i

# Remove All Empty Points
super + shift + a
    bsp-empty-remove

# Insert Focused Window in Empty Point
super + a
    bspc node $(bspc query -N -n focused) -n $(bspc query -N -n .leaf.\!window)

# Rotate -90 Degree
super + alt + minus
    bspc node @parent -R -90

# Rotate +90 Degree
super + alt + equal
    bspc node @parent -R 90

# Next Workspace
super + alt + shift + Right
    bspc desktop -f next

# Next Workspace
super + alt + shift + Left
    bspc desktop -f prev

# Swap Focus to The Largest Window and back (Zoom)
super + s
    bsp-zoom

# Swap Focus to The Second Largest Window and back (Zoom)
super + alt + s
    bsp-zoom-second_biggest

# Delete BSP Cache
super + shift + ctrl + Escape
    rm -f "$HOME/.cache/bsp"* 2>/dev/null; notify-send "BSP Cache Removed"

# Toggle Gaps On/Off
super + ctrl + shift + equal
    bsp-gaps-toggle

# Toggle Borders On/Off
super + shift + ctrl + b
    bsp-border-toggle

# Border Size Increase
super + ctrl + b
    bsp-border-size inc

# Border Size Decrease
super + ctrl + alt + b
    bsp-border-size dec

# Border Color Cycle (Next)
super + shift + b
    bsp-border-color next

# Border Color Cycle (Previous)
super + shift + alt + b
    bsp-border-color prev

# Cycle Feh Wallpapers
super + shift + w
    feh-cycle next

# Cycle Feh Wallpapers
super + shift + alt + w
    feh-cycle prev

# Color Picker
super + y
    sh -c 'color=$(xcolor); printf "%s" "$color" | xclip -selection clipboard; notify-send "Color" "$color" -h string:bgcolor:"$color" -h string:fgcolor:"#fff"'

# Circle Zoom (sxcs)
super + u
    sxcs --mag-filters "circle"

# Full Zoom (xmagnify)
super + alt + u
    magnify -wexpr 1920 / 4 -hexpr 1080 / 4 -m4 -r30

# Full Zoom (xzoom)
super + shift + u
    xzoom -mag 2

